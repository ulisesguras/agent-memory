# Agent Memory — Dependencies
# Core engine has ZERO external deps.
# Add only what you use.

# Web server (optional)
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0

# MCP Server (optional — for agent discovery)
# pip install mcp

# LLM Backends (install only what you use)
# pip install anthropic
# pip install google-cloud-aiplatform

# Dev/testing
pytest>=8.0.0
httpx>=0.27.0
